name: 📦 Обновить каталог из Google Sheets

on:
  workflow_dispatch:

jobs:
  update-catalog:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Даем права на запись в репозиторий

    steps:
      - name: 📥 Клонирование репозитория
        uses: actions/checkout@v4

      - name: ⬇️ Скачивание CSV
        run: |
          mkdir -p assets
          curl -L "https://docs.google.com" -o assets/products.csv

      - name: 🚀 Сохранение изменений в main
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add assets/products.csv
          # Коммитим только если файл изменился, чтобы не было ошибки
          git diff --quiet && git diff --staged --quiet || (git commit -m "auto: обновление каталога" && git push)
